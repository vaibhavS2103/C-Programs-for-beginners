#include <stdio.h>
int main() {
    int num;
    printf("Enter an integer: ");
  cin>>num

    // True if num is perfectly divisible by 2
    if(num % 2 == 0)
        cout<<"Even no";
    else
        cout<<"Odd No";
    
    return 0;
}
